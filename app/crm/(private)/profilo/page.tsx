import { createServerSupabaseClient } from "@/lib/supabase/server";
import { updateProfile } from "./actions";
export default async function ProfilePage(){const s=await createServerSupabaseClient();const {data:{user}}=await s.auth.getUser();const {data:p}=await s.from("profiles").select("*").eq("id",user!.id).single();return <><p className="eyebrow">Account</p><h1>Il tuo profilo</h1><form action={updateProfile} className="card" style={{maxWidth:680}}><div className="field"><label>Email</label><input value={p?.email??""} disabled/></div><div className="field"><label>Nome completo</label><input name="fullName" defaultValue={p?.full_name} required/></div><div className="field"><label>Presentazione WhatsApp</label><input name="presentation" defaultValue={p?.presentation??""} placeholder="del corso Astraios 19-22 del Morosini"/><small className="muted">Comparirà dopo il tuo nome nei messaggi.</small></div><button className="button">Salva profilo</button></form></>}
